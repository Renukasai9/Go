package main

import (
	"fmt"
)

// decimal,hexadecimal,binary representation of a number
const (
	a = 24
)

func main() {
	fmt.Printf("%d in decimal \t %b in binary \t %x in hexadecimal", a, a, a)
}
